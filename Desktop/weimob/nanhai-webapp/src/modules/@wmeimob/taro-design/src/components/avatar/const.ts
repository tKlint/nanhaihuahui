/**
 * 形状
 *
 * @export
 * @enum {number}
 */
export enum EMMAvatarShape {
    Circle = 'circle',
    Square = 'square',
}

/**
 * 大小
 *
 * @export
 * @enum {number}
 */
export enum EMMAvatarSize {
    Large = 'large',
    Default = 'default',
    Small = 'small'
}
